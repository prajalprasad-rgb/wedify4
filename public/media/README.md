Recommended production media paths:

- `public/media/wedding-hero.mp4`
- Gallery and reel uploads should use the `media-assets` Supabase Storage bucket.

The landing page includes remote fallback posters so the project remains previewable before local media is added.
